# TDD Рефакторинг План

**Дата:** 09.04.2026  
**Статус:** Ожидает утверждения

---

## Итоги аудита

| Метрика | Значение |
|---------|----------|
| 🔴 Критично | 5 |
| 🟡 Средне | 5 |
| 🟢 OK | ~80 |

---

## 🔴 ЭТАП 1: Критичные файлы (5 файлов)

### 1.1 `RecommendationsClient.tsx`

| Метрика | Значение |
|---------|----------|
| Строк | 900+ |
| If | 50+ |
| Принципы | SOLID-S, KISS |

**Проблемы:**
- Множественная ответственность (SRP нарушен)
- Функции >20 строк
- Вложенные if >3 уровней

**TDD план:**
```
RED: Написать тест на разбиение компонента
GREEN: Минимальная реализация — вынести хуки useFetchRecommendations, useSessionTracking в отдельные файлы
REFACTOR: Применить KISS — упростить условия, DRY — вынести общие функции
```

---

### 1.2 `lib/taste-map/compute.ts`

| Метрика | Значение |
|---------|----------|
| Строк | 400+ |
| If | 20+ |

**Проблемы:**
- Одна функция computeGenreCounts + computeGenreProfile делают слишком много
- Дублирование логики normalizeGenreName

**TDD план:**
```
RED: Тест на отдельные функции computeGenreProfile и computeGenreCounts
GREEN: Разделить функции, DRY — вынести normalizeGenreName
REFACTOR: Проверить YAGNI — убрать неиспользуемые параметры
```

---

### 1.3 `lib/recommendation-algorithms/taste-match.ts`

| Метрика | Значение |
|---------|----------|
| Строк | 350+ |
| If | 15+ |

**TDD план:**
```
RED: Тест на execute() с моком getSimilarUsers
GREEN: Минимальная реализация алгоритма
REFACTOR: Применить DRY — вынести normalizeScores, weights в константы
```

---

### 1.4 `lib/recommendation-algorithms/person-recommendations.ts`

| Метрика | Значение |
|---------|----------|
| Строк | 300+ |
| If | 12+ |

**TDD план:**
```
RED: Тест на execute() с моком getSimilarUsers
GREEN: Минимальная реализация
REFACTOR: DRY — вынести общую логику с taste-match в shared/interface
```

---

### 1.5 `lib/recommendation-algorithms/genre-recommendations.ts`

| Метрика | Значение |
|---------|----------|
| Строк | 250+ |
| If | 10+ |

**TDD план:**
```
RED: Тест на execute() 
GREEN: Минимальная реализация
REFACTOR: DRY — общая логика с person-recommendations
```

---

## 🟡 ЭТАП 2: Средние файлы (5 файлов)

| # | Файл | Строк | Ifs | Принцип | Проблема | TDD План |
|---|------|-------|-----|---------|----------|----------|
| 2.1 | `person-comparison.ts` | 200+ | 8 | KISS | Вложенные if | Упростить условия через early return |
| 2.2 | `genre-twins.ts` | 180+ | 7 | DRY | Похож на type-twins | Вынести общую логику в shared |
| 2.3 | `type-twins.ts` | 150+ | 6 | DRY | Похож на genre-twins | → |
| 2.4 | `drop-patterns.ts` | 150+ | 5 | YAGNI | Неиспользуемые функции | Удалить мёртвый код |
| 2.5 | `api/recommendations/route.ts` | 140+ | 5 | KISS | Сложная валидация | Упростить через схему |

---

## 🟢 ЭТАП 3: Остальные файлы (~80)

**Действия:**
1. Проверить покрытие тестами
2. Запустить `npx tsc --noEmit` — 0 errors
3. Запустить `npx vitest run` — регрессий нет

---

## Принципы рефакторинга

| Принцип | Описание | Применение |
|---------|----------|------------|
| **SOLID-S** | Одна функция — одна ответственность | Все этапы |
| **KISS** | Простое решение — правильное | Все этапы |
| **DRY** | Не повторять код | Этапы 1, 2 |
| **YAGNI** | Не добавлять "на будущее" | Этап 2 |

---

## Чеклист завершения

```
□ Этап 1: Все 5 критичных файлов refactored
□ Этап 2: Все 5 средних файлов refactored  
□ Этап 3: Тесты покрытия >80%
□ TypeScript: 0 errors
□ vitest: регрессий нет
□ git commit: refactor: TDD refactoring complete
```

---

## Нотатки

- Начинать с Этапа 1.1 только после утверждения плана
- Каждый файл — отдельный TDD цикл (RED → GREEN → REFACTOR)
- После каждого этапа запускать lint + tests
- Коммиты после каждого этапа
