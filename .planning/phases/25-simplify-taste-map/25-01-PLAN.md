---
phase: 25-simplify-taste-map
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/taste-map/similarity.ts
  - src/lib/taste-map/similarity-storage.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - Similarity scores computed with 60% genres + 40% movies weights
    - personOverlap removed from similarity calculations
    - TasteMap snapshot no longer includes personProfiles
  artifacts:
    - path: src/lib/taste-map/similarity.ts
      provides: Similarity calculation with updated weights
      contains: WEIGHTS.ratingCorrelation = 0.4
      contains: WEIGHTS.tasteSimilarity = 0.6
    - path: src/lib/taste-map/similarity-storage.ts
      provides: Snapshot generation without personProfiles
      contains: generateTasteMapSnapshot returns only genreProfile
  key_links:
    - from: src/lib/taste-map/similarity.ts
      to: computeOverallMatch
      via: function call
      pattern: WEIGHTS\.(ratingCorrelation|tasteSimilarity)
---

<objective>
Update similarity weights and remove person overlap from calculations.
</objective>

<execution_context>
@C:/Users/n0rds/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@src/lib/taste-map/similarity.ts
@src/lib/taste-map/similarity-storage.ts
</context>

<tasks>

<task type="auto">
  <name>Update similarity weights to 60% genres + 40% movies</name>
  <files>src/lib/taste-map/similarity.ts</files>
  <action>
    Update WEIGHTS constant (lines 49-53):
    - Change ratingCorrelation from 0.5 to 0.4 (Movies)
    - Change tasteSimilarity from 0.3 to 0.6 (Genres)
    - Remove personOverlap (set to 0 or remove from weights)
    
    Update computeOverallMatch function (lines 362-373):
    - Remove personOverlap from the weighted sum calculation
    - Formula: movieScore * 0.4 + tasteSimilarity * 0.6
    
    Update SimilarityResult interface (line 91-98):
    - Keep personOverlap field (for backward compat) but mark it will always be 0
    
    Update personOverlap function (lines 329-350):
    - Keep function but it will no longer be called from computeSimilarity
    
    Update comments that reference old weights (50/30/20).
  </action>
  <verify>npm run lint passes, TypeScript compiles without errors</verify>
  <done>Similarity weights updated: Genres 60%, Movies 40%. personOverlap removed from overallMatch calculation.</done>
</task>

<task type="auto">
  <name>Update similarity-storage snapshot</name>
  <files>src/lib/taste-map/similarity-storage.ts</files>
  <action>
    Update generateTasteMapSnapshot function (lines 252-260):
    - Remove personProfiles from the returned snapshot object
    - Only return genreProfile (for reproducibility of genre-based similarity)
    
    Before:
    ```typescript
    return {
      genreProfile: tasteMap.genreProfile,
      personProfiles: {
        actors: tasteMap.personProfiles.actors,
        directors: tasteMap.personProfiles.directors,
      },
    };
    ```
    
    After:
    ```typescript
    return {
      genreProfile: tasteMap.genreProfile,
    };
    ```
  </action>
  <verify>npm run lint passes</verify>
  <done>generateTasteMapSnapshot only includes genreProfile, not personProfiles.</done>
</task>

</tasks>

<verification>
- Run: npm run lint
- Run: npx tsc --noEmit
- Verify computeOverallMatch formula: movieScore * 0.4 + tasteSimilarity * 0.6
</verification>

<success_criteria>
- WEIGHTS.ratingCorrelation = 0.4
- WEIGHTS.tasteSimilarity = 0.6
- personOverlap not used in overallMatch calculation
- generateTasteMapSnapshot returns only genreProfile
</success_criteria>

<output>
After completion, create `.planning/phases/25-simplify-taste-map/25-01-SUMMARY.md`
</output>
