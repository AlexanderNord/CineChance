---
phase: 19-testing-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vitest.config.ts
  - package.json
autonomous: true
requirements: []
must_haves:
  truths:
    - "Tests run with coverage reporting"
    - "Coverage thresholds enforced on CI"
  artifacts:
    - path: vitest.config.ts
      provides: Test configuration with coverage thresholds
    - path: package.json
      provides: Test scripts including coverage
  key_links: []
---

<objective>
Configure testing infrastructure with coverage thresholds to enable 80%+ test coverage goal.
</objective>

<tasks>

<task type="auto">
  <name>Configure Vitest with coverage thresholds</name>
  <files>vitest.config.ts</files>
  <action>
    Update vitest.config.ts to add coverage configuration with thresholds:
    - lines: 80
    - functions: 80
    - branches: 75
    - statements: 80
    
    Use v8 provider for coverage.
  </action>
  <verify>npm run test:ci passes with coverage thresholds</verify>
  <done>Vitest configured with coverage thresholds</done>
</task>

<task type="auto">
  <name>Add test:coverage script to package.json</name>
  <files>package.json</files>
  <action>
    Add "test:coverage" script to package.json that runs vitest with --coverage flag.
  </action>
  <verify>npm run test:coverage runs and produces coverage report</verify>
  <done>Coverage script available in package.json</done>
</task>

</tasks>

<verification>
Run npm run test:ci to verify configuration works
</verification>

<success_criteria>
- [ ] vitest.config.ts has coverage thresholds configured
- [ ] package.json has test:coverage script
- [ ] Tests run successfully with coverage reporting
</success_criteria>

<output>
After completion, create .planning/phases/19-testing-foundation-increase-test-coverage-to-80/19-01-SUMMARY.md
</output>
