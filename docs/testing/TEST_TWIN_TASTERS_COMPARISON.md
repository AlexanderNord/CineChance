# 🧪 Инструкции Тестирования: Исправление Twin Tasters (2025-01-27)

## 🎯 Что было исправлено?

**Проблема:** Страница сравнения вкусов показывала ошибку "Failed to load comparison {}"

**Причина:** Next.js 16 передаёт параметры как Promise, но API route не ждал их разрешения

**Исправления:**
1. ✅ Обновлена типизация params в route handler
2. ✅ Добавлено `await Promise.resolve(params)`
3. ✅ Улучшена обработка ошибок с детальными логами
4. ✅ Более информативные сообщения об ошибках на клиенте

---

## 📋 Чеклист Тестирования

### 1️⃣ Базовая Проверка (5 мин)

```bash
# Запустить dev сервер
npm run dev

# Откроется на http://localhost:3000
```

**Шаги:**
1. Авторизоваться в приложении
2. Перейти на `/profile/taste-map` (главная профиля с вкусовой картой)
3. Визуально убедиться что карточки Twin Tasters загружены

**Ожидаемый результат:**
- Страница загружается без ошибок
- Видны карточки похожих пользователей
- Отсутствуют ошибки в консоли

---

### 2️⃣ Функциональная Проверка (10 мин)

**Шаги:**
1. Кликнуть на одну из карточек Twin Tasters
2. Должна открыться страница сравнения на `/profile/taste-map/compare/[userId]`
3. Страница должна загрузиться с метриками

**Ожидаемый результат:**
- ✅ Навигация произошла без ошибок
- ✅ Страница сравнения загрузилась
- ✅ Видны четыре метрики (Taste, Rating, Person, Overall)
- ✅ Видны статистики просмотров
- ✅ Видны общие фильмы (если они есть)
- ✅ **ВАЖНО:** Никаких ошибок в браузер консоли

**Как проверить консоль:**
1. Открыть DevTools: F12 или Cmd+Option+I
2. Перейти на вкладку "Console"
3. Должны видеть `success` логи, НЕ `error` или `warn`

---

### 3️⃣ Проверка API (5 мин)

Запустить встроенный тест:

```bash
node test-comparison-api.js
```

**Ожидаемый результат:**
```
🧪 Testing Taste Map Comparison API...

1️⃣  Fetching similar users...
✅ Got N similar users

2️⃣  Testing comparison API with user: xxx
   (Match: Y%)
✅ Successfully retrieved comparison data

📊 Comparison Metrics:
   Taste Similarity: A%
   Rating Correlation: B%
   Person Overlap: C%
   Overall Match: D%

📈 Watch Statistics:
   Your watched movies: N1
   Their watched movies: N2
   Shared movies: N3
   Shared movies in response: N4

🎬 Sample shared movies:
   - Movie Title 1
   - Movie Title 2

✅ API test completed successfully!
```

**Если что-то не работает:**
- Убедиться что dev сервер запущен
- Проверить логи сервера терминала
- Часть ошибок может быть в `stdout` dev сервера

---

### 4️⃣ Проверка Обработки Ошибок (5 мин)

**Тест 1: Несуществующий пользователь**
1. Открыть DevTools Console
2. Вставить и выполнить:
   ```javascript
   fetch('/api/user/taste-map-comparison/nonexistent-id')
     .then(r => r.json())
     .then(console.log)
     .catch(console.error)
   ```
3. Должно вернуться: `{ error: "...", details: "..." }`
4. **НЕ** пустой object `{}`

**Тест 2: Неавторизованный доступ**
1. Открыть приватное окно браузера (инкогнито)
2. Перейти на `/profile/taste-map/compare/any-user-id`
3. Должно редиректить на страницу входа
4. **НЕ** показывать ошибку сравнения

---

### 5️⃣ Проверка Логирования (10 мин)

**На сервере (terminal где запущен npm run dev):**

1. Запустить тест: `node test-comparison-api.js`
2. Посмотреть логи в терминале с dev сервером
3. Должны видеть записи типа:

```
[COMPARISON_API] Comparison request: userId=xxx comparedUserId=yyy
[COMPARISON_API] Found N shared movies
[COMPARISON_API] Returned comparison data successfully
```

**На клиенте (браузер Console):**

1. F12 → Console
2. Зайти на страницу сравнения
3. Должны видеть логи типа:
```
[ComparisonPage] Successfully loaded comparison
[ComparisonPage] Metrics: { tasteSimilarity: ..., ... }
```

**Если видите ошибки вместо успехов:**
- Это значит что исправление ещё не полностью работает
- Проверить был ли dev сервер перезагружен после изменений
- Проверить консоль на наличие syntax errors

---

## 🚀 Полная Пользовательская История Тестирования

### Сценарий A: Типичный путь (15 мин)

```
1. Авторизация
2. Перейти на /profile/taste-map
3. Увидеть список Twin Tasters  ✅
4. Кликнуть на первую карточку ✅
5. Открылась страница сравнения ✅
6. Данные загружены успешно ✅
7. Видны все метрики ✅
8. Видны общие фильмы ✅
9. Нажать "Назад", вернуться на карту ✅
10. Кликнуть на другую карточку ✅
11. Вся процедура повторилась успешно ✅
```

### Сценарий B: Edge Cases (10 мин)

```
1. Кликнуть на Twin Taster с 100% совпадением
   → Должны видеть очень похожие метрики ✅

2. Кликнуть на Twin Taster с низким совпадением (~25%)
   → Должны видеть сильные различия в метриках ✅

3. Если вообще нет Twin Tasters
   → Страница должна показать "No similar users found" ✅
```

---

## 🐛 Если Что-то Сломалось

### ❌ Ошибка: "Failed to load comparison {}"

**Решение:**
1. Проверить был ли dev сервер полностью перезагружен
2. Кликнуть Refresh в браузере (Ctrl+Shift+R / Cmd+Shift+R)
3. Проверить логи сервера в терминале
4. Запустить `npm run test:ci` чтобы найти синтаксис ошибки

### ❌ Ошибка: "Пользователь не найден"

**Возможные причины:**
1. Параметр userId неправильно передаётся в URL
2. Пользователь действительно не существует в БД
3. Есть проблема с авторизацией

**Решение:**
1. Проверить URL в адресной строке браузера
2. Убедиться что пользователь есть в БД (проверить через админ панель)
3. Проверить логи сервера

### ❌ "Ошибка сервера при расчёте сравнения"

**Это означает:**
1. API вернул статус 500
2. Что-то пошло не так при расчёте метрик

**Решение:**
1. Проверить логи сервера (будут детали в stderr)
2. Убедиться что у обоих пользователей есть watched movies
3. Перезагрузить dev сервер: Ctrl+C и `npm run dev`

---

## ✅ Критерии Успеха

### Минимальное (Must Have)
- [ ] Страница сравнения загружается без ошибок
- [ ] Видны все 4 метрики
- [ ] Кликабельны Twin Taster карточки
- [ ] Нет ошибок в браузер консоли

### Стандартное (Should Have)
- [ ] Видны общие фильмы
- [ ] Видны различия в рейтингах
- [ ] Работает кнопка "Назад"
- [ ] API test проходит полностью

### Оптимальное (Nice to Have)
- [ ] Логирование в серверном терминале показывает детали
- [ ] Error messages информативны (указывают на проблему)
- [ ] Плавное UX при навигации между сравнениями

---

## 📊 Отчётность

После тестирования:

1. **Если всё работает:**
   - Маркировать как **RESOLVED** 
   - Удалить test-comparison-api.js (уже не нужен)
   - Обновить docs/bugs/README.md (бага решена)

2. **Если есть проблемы:**
   - Собрать логи сервера и браузера
   - Создать новый issue в docs/bugs/
   - Запустить GSD debug: `/gsd-debug "Comparison page still errors with..."`

---

**Начало тестирования:** 2025-01-27  
**Последний обновление:** 2025-01-27  
**Статус:** READY FOR TESTING
