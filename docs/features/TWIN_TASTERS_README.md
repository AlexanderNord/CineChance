# 👥 Twin Tasters - Близнецы вкуса

## 📌 ЧТО ЭТО?

Система, которая находит других пользователей CineChance с похожим профилем предпочтений фильмов.

На странице `/profile/taste-map` вы увидите список "близнецов" - киноманов, которые смотрят то же самое и оценивают также как вы.

```
┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃ Ваши близнецы вкуса                    ┃
┣━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┫
┃ 👤 Киномана AB12 • 42 фильма • 6м     ┃
┃    Сходство вкуса: [▓▓▓▓▓▓▓▓░] 85%    ┃
┣━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┫
┃ 👤 Киномана CD34 • 150 фильмов • 1г   ┃
┃    Сходство вкуса: [▓▓▓▓▓▓░░░░] 72%   ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛
```

> **⚠️ UPDATE (2026-02-24):** Найден и исправлен критический баг! Функция `isSimilar()` проверяла только жанры, пропуская рейтинги и актеров. Теперь система использует полное комбинированное сходство (`overallMatch`). [Читайте подробнее](./docs/bugs/2026-02-24-similar-users-not-found.md)

---

## 📂 ГДЕ НАЙТИ?

### Для пользователей
```
http://localhost:3000/profile/taste-map
                      ↓
            (Скролльте вниз)
                      ↓
          Ваши близнецы вкуса
```

### Для разработчиков
```
Документация:
├─ QUICK_START.md ────────────── Начните отсюда! (5 мин)
├─ docs/features/twin-tasters.md ─── Архитектура
├─ docs/features/twin-tasters-testing.md ──── Как тестировать
├─ docs/features/twin-tasters-analysis.md ─── Анализ

Код:
├─ src/app/api/user/similar-users/route.ts ───── API endpoint
├─ src/app/profile/taste-map/TwinTasters.tsx ─── UI компонент
└─ src/lib/taste-map/similarity.ts ───────────── Алгоритм
```

---

## 🔥 БЫСТРЫЙ СТАРТ (5 минут)

### 1. Запустить сервер
```bash
npm run dev
```

### 2. Открить страницу
```
http://localhost:3000/profile/taste-map
```

### 3. Найти раздел "Ваши близнецы вкуса"
- Если < 5 фильмов в фильмотеке → раздел скрыт
- Если ≥ 5 фильмов → будет список похожих пользователей

### 4. Готово! 🎉

---

## ⚙️ КАК ЭТО РАБОТАЕТ?

### Три компонента системы

#### 1. **API** (`/api/user/similar-users`)
```
GET /api/user/similar-users?limit=10&useCache=true
     ↓
Проверяет Redis кэш (24h TTL)
     ├─ НАЙДИ → вернуть результаты
     └─ НЕ НАЙДИ → вычислить → сохранить → вернуть
```

#### 2. **Алгоритм** (similarity.ts)
Вычисляет сходство по 3 метрикам:

| Метрика | Вес | Что | Формула |
|---------|---|---|---|
| **Жанры** | 50% | Профиль предпочтений | Cosine similarity |
| **Оценки** | 30% | Как вы оцениваете общие фильмы | Pearson correlation |
| **Актеры** | 20% | Любимые персоналии | Jaccard similarity |

**Итог:** `match = жанры*0.5 + оценки*0.3 + актеры*0.2`

#### 3. **UI** (TwinTasters.tsx)
```
TasteMapClient
  └─ TwinTasters
      ├─ Загрузка
      ├─ Вызов API
      └─ Отображение карточек
```

---

## 🎯 ЧТО БЫЛО СДЕЛАНО

### Было в системе ✅
- Функции для вычисления сходства (`similarity.ts`)
- Несколько алгоритмов близнецов (`type-twins.ts`, `person-twins.ts`)
- Кэширование в Redis

### Проблемы ❌
- Нет API для получения близнецов
- Нет UI компонента на странице profile  
- Рейтинговая корреляция не вычислялась (была всегда 0)
- Требовалось передавать список кандидатов

### Что добавили ✅
1. **API endpoint** - `GET /api/user/similar-users`
2. **UI компонент** - `TwinTasters.tsx` на странице profile
3. **Функция корреляции** - `computeRatingCorrelation()` (теперь работает!)
4. **Auto discovery** - API сам находит кандидатов

---

## 📊 ПРИМЕРЫ

### Пример 1: Идеальное совпадение
```
Вы:
  • Жанры: Комедия (80), Драма (60)
  • Оценили фильм "Зеленая миля": 9/10
  
Они:
  • Жанры: Комедия (75), Драма (65)
  • Оценили фильм "Зеленая миля": 8/10

Результат: 85% match ✅
```

### Пример 2: Противоположные вкусы
```
Вы:
  • Жанры: Комедия (90)
  • Оценки: 9-10
  
Они:
  • Жанры: Триллер (90)
  • Оценки: 2-3 их же комедиям

Результат: 15% match ❌
```

### Пример 3: Недостаточновой данных
```
Вы: 3 фильма
Результат: Twin Tasters не показан (нужно ≥ 5)
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Способ 1: Веб интерфейс
```
1. npm run dev
2. Залогиниться
3. Добавить 5+ фильмов
4. Перейти на /profile/taste-map
5. Скролльте вниз → найрите близнецов
```

### Способ 2: API напрямую
```bash
# Получить 10 похожих пользователей
curl "http://localhost:3000/api/user/similar-users?limit=10"

# Ответ:
{
  "similarUsers": [
    {
      "userId": "user_id_123",
      "overallMatch": 78.5,
      "watchCount": 42,
      "memberSince": "2025-06-15T10:30:00Z"
    }
  ],
  "cached": false,
  "computedAt": "2026-02-24T15:35:00Z"
}
```

### Способ 3: DevTools браузера
```javascript
// F12 → Console:
fetch('/api/user/similar-users?limit=5')
  .then(r => r.json())
  .then(d => console.log(d))
```

---

## 📚 ДОКУМЕНТАЦИЯ

| Файл | Назначение | Для кого |
|------|-----------|---------|
| **TWIN_TASTERS_QUICK_START.md** | За 30 секунд | Все |
| **docs/features/twin-tasters.md** | Полная архитектура | Разработчики |
| **docs/features/twin-tasters-testing.md** | Как тестировать | QA/Разработчики |
| **docs/features/twin-tasters-analysis.md** | Что было/изменилось | Разработчики |
| **IMPLEMENTATION_CHECKLIST.md** | Полный чек-лист | Разработчики |
| **этот файл** | Обзор | Все |

---

## 📝 ОСНОВНЫЕ ФАЙЛЫ

### Новые файлы (3)
```
src/app/api/user/similar-users/route.ts
      ├─ API endpoint
      ├─ Rate limiting
      ├─ Кэширование (Redis)
      └─ Обработка ошибок

src/app/profile/taste-map/TwinTasters.tsx
      ├─ React компонент
      ├─ Loading/error states
      ├─ Красивые карточки
      └─ Auto-load при монтировании

src/lib/taste-map/similarity.ts (обновлен)
      └─ + computeRatingCorrelation()
            ├─ Находит общие фильмы
            ├─ Вычисляет Pearson correlation
            └─ Теперь работает! (было 0)
```

### Изменённые файлы (1)
```
src/app/profile/taste-map/TasteMapClient.tsx
      └─ + <TwinTasters userId={userId} />
```

---

## ✨ ОСОБЕННОСТИ

### Преимущества
- ✅ Автоматический поиск похожих пользователей
- ✅ Красивое отображение на странице
- ✅ Кэширование на 24 часа
- ✅ Rate limiting (защита от спама)
- ✅ Обработка ошибок
- ✅ Анонимные данные (только ID)

### Ограничения
- ⚠️ Требует ≥ 5 фильмов у пользователя
- ⚠️ Проверяет только 100 активных пользователей
- ⚠️ Обновляется раз в 24 часа
- ⚠️ Требует Redis для кэшеing

---

## 🚀 ГОТОВНОСТЬ

| Компонент | Статус | Примечание |
|-----------|--------|-----------|
| API | ✅ | Работает, протестировано |
| UI | ✅ | Интегрировано, отрендерено |
| Алгоритм | ✅ | Исправлена корреляция |
| Кэширование | ✅ | Redis работает |
| Rate limiting | ✅ | Применяется |
| Документация | ✅ | 5 файлов |
| TypeScript | ✅ | Без ошибок |

**Вердикт:** ✅ **ГОТОВО К PRODUCTION**

---

## 📞 ПОМОЩЬ

### Если не видно "Близнецов вкуса"
1. Проверьте количество фильмов (≥ 5?)
2. Обновите страницу (F5)
3. Проверьте консоль (F12 → Console)
4. Проверьте сервер логи (терминал)

### Если API возвращает ошибку
1. Проверьте Auth (залогинены ли?)
2. Проверьте Redis (запущен ли?)
3. Проверьте сетьевые ошибки (консоль)

### Если "No similar users found"
1. Может быть только вы в системе
2. Может быть никто похож на вас
3. Может быть у остальных < 5 фильмов
4. Подождите 24 часа (кэш обновляется)

---

## 🎓 ДОПОЛНИТЕЛЬНО

Хотите узнать больше?
- Читайте `TWIN_TASTERS_QUICK_START.md` (вся информация за 5 минут)
- Смотрите `docs/features/twin-tasters.md` (полная архитектура)
- Запустите тесты из `docs/features/twin-tasters-testing.md`

---

## 🎉 ГОТОВО!

Система "Близнецы вкуса" полностью реализована и готова к использованию.

**Начните работу:**
```bash
npm run dev
# Откройте http://localhost:3000/profile/taste-map
# Скролльте вниз → найдите похожих пользователей
```

Спасибо за внимание! 🚀
